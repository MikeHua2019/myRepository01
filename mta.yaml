_schema-version: "3.1"
ID: MK_OFFICE_SUPPLIES
description: A simple CAP project.
version: 1.0.0
modules:
- name: MK_OFFICE_SUPPLIES-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: MK_OFFICE_SUPPLIES-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm-ci
- name: MK_OFFICE_SUPPLIES-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: MK_OFFICE_SUPPLIES-db
  parameters:
    buildpack: nodejs_buildpack
- name: MK_OFFICE_SUPPLIES-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: MK_OFFICE_SUPPLIES-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mikeofficesuppliesmkfiori.zip
      name: mikeofficesuppliesmkfiori
      target-path: resources/
- name: mikeofficesuppliesmkfiori
  type: html5
  path: app/mkfiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: MK_OFFICE_SUPPLIES-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-service-name: ${service-name}
- name: MK_OFFICE_SUPPLIES-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: MK_OFFICE_SUPPLIES-html5-srv
    service-plan: app-host
- name: MK_OFFICE_SUPPLIES-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: MK_OFFICE_SUPPLIES-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx -p @sap/cds-dk cds build --production
