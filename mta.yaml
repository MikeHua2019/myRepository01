_schema-version: '3.1'
ID: MK_OFFICE_SUPPLIES
version: 2.0.0
description: A simple CAP project.
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom 
      commands:
        - npm install --production
        - npx -p @sap/cds-dk cds build --production
modules:
# --------------------- SERVER MODULE ------------------------
  - name: MK_OFFICE_SUPPLIES-srv
# ------------------------------------------------------------  
    type: nodejs
    path: gen/srv
    requires:
      - name: MK_OFFICE_SUPPLIES-db
      - name: MK_OFFICE_SUPPLIES-xsuaa
      - name: MK_OFFICE_SUPPLIES-destination
    provides:
      - name: srv-api
        properties:
          srv-url: '${default-url}'
 # --------------------- SIDECAR MODULE ------------------------         
  - name: MK_OFFICE_SUPPLIES-db-deployer
 # ------------------------------------------------------------   
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: MK_OFFICE_SUPPLIES-db
      - name: MK_OFFICE_SUPPLIES-xsuaa
# ------------------------------------------------------------      
  - name: MK_OFFICE_SUPPLIES-approuter
# ------------------------------------------------------------  
    type: nodejs
    path: approuter
    requires:
      - name: MK_OFFICE_SUPPLIES-xsuaa
      - name: srv-api
        group: destinations
        properties:
          forwardAuthToken: true
          strictSSL: true
          name: srv-binding
          url: '~{srv-url}'
    build-parameters:
          requires:
            - name: MK_OFFICE_SUPPLIES-app
              artifacts:
                - ./*
              target-path: resources
# ------------------------------------------------------------
  - name: MK_OFFICE_SUPPLIES-app
# ------------------------------------------------------------  
    type: html5
    path: app
    build-parameters:
      supported-platforms: []
resources:
# ------------------------------------------------------------
  - name: MK_OFFICE_SUPPLIES-db
# ------------------------------------------------------------  
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
    properties:
      hdi-service-name: '${service-name}'
# ------------------------------------------------------------
  - name: MK_OFFICE_SUPPLIES-xsuaa
# ------------------------------------------------------------
    type: org.cloudfoundry.managed-service
    parameters:
      service-plan: application
      service: xsuaa
      path: ./xs-security.json
      config:
        xsappname: 'MK_OFFICE_SUPPLIES-${space}'
        role-collections:
          - name: 'Vendor-${space}'
            description: Read Access
            role-template-references:
              - $XSAPPNAME.Vendor
          - name: 'ProcurementManager-${space}'
            description: All Access
            role-template-references:
              - $XSAPPNAME.ProcurementManager
# ------------------------------------------------------------  
  - name: MK_OFFICE_SUPPLIES-destination
# ------------------------------------------------------------  
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite